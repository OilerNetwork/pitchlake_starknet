{
  "name": "pitchlake_starknet_ci",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type":"module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-abis": "npx abi-wan-kanabi --input ../target/dev/pitch_lake_starknet_OptionRound.contract_class.json --output ./abi/optionRound.ts && npx abi-wan-kanabi --input ../target/dev/pitch_lake_starknet_Vault.contract_class.json --output ./abi/vault.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "starknet": "^6.10.2",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
