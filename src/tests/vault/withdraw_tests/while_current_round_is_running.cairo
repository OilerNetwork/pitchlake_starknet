// Withdraw should come from current round and next if needed
// Test vault next position for LP decremented (if needed), checkpoint untouched
// Test vault unallocated for LP decremented
// Test current and (if needed) next round's unallocated decremeneted
// Test a vault storage var is updated to reflect how much LP collects (premiums/unsold) from the current round


