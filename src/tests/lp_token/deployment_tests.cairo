// @note Add test for lp token constructor
//  - token should know the vault & option round addresses (mk agg too ?)
//  - token should know what round it comes from 
//    - should discuess name/symbol for each token, PLPT1, Pitchlake LP Token 1 ? (low priority)

// @note Add test that only the vault can mint/burn tokens


